version: '3.9'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
      # Optional: move secrets to env vars instead of source
      # NEXT_PUBLIC_AIRTABLE_TOKEN: "${NEXT_PUBLIC_AIRTABLE_TOKEN}"
      # NEXT_PUBLIC_AIRTABLE_BASE_ID: "${NEXT_PUBLIC_AIRTABLE_BASE_ID}"
      # NEXT_PUBLIC_AIRTABLE_TABLE: "${NEXT_PUBLIC_AIRTABLE_TABLE}"
    restart: unless-stopped
